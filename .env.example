# Vercel KV环境变量示例
# 如果你想在本地开发时也使用云端存储，请配置这些变量

# Vercel KV REST API配置（推荐）
KV_REST_API_URL=your_kv_rest_api_url
KV_REST_API_TOKEN=your_kv_rest_api_token

# 或者使用Redis URL（备选）
REDIS_URL=redis://default:password@host:port

# 注意：
# 1. 如果不配置这些变量，本地开发将自动使用localStorage
# 2. 部署到Vercel时，系统会自动使用云端存储
# 3. 复制此文件为.env.development.local并填入真实值