# 图片生成配置说明

## 1. 支持的图片生成服务

本项目支持多种图片生成服务：

1. **OpenAI DALL-E**
2. **Gemini Flash Image**
3. **其他兼容OpenAI API的文生图服务**

## 2. 配置步骤

### 2.1 获取API密钥

#### OpenAI DALL-E
1. 访问 [OpenAI官网](https://platform.openai.com/)
2. 注册账号并完成支付设置
3. 在API Keys页面创建新的密钥
4. 保存密钥以备使用

#### Gemini Flash Image
1. 访问 [Google AI Studio](https://aistudio.google.com/)
2. 创建项目并获取API密钥
3. 保存密钥以备使用

### 2.2 在项目中配置API

#### 方法一：通过UI界面配置（推荐）
1. 启动项目应用
2. 进入设置页面
3. 找到"图片生成"或"图像模型"配置区域
4. 填写以下信息：
   - **提供商**: 选择图片生成服务提供商（openai/gemini/其他）
   - **模型**: 选择要使用的模型
     - OpenAI: `dall-e-3` 或 `dall-e-2`
     - Gemini: `gemini-flash-image`
   - **API密钥**: 输入你获取的API密钥
   - **基础URL**（可选）: 如果使用代理或自定义端点，填写相应URL

#### 方法二：通过环境变量配置
在项目根目录创建或编辑 `.env.local` 文件，添加以下内容：

```env
# OpenAI配置示例
IMAGE_PROVIDER=openai
IMAGE_MODEL=dall-e-3
IMAGE_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
IMAGE_BASE_URL=https://api.openai.com/v1

# Gemini配置示例
IMAGE_PROVIDER=gemini
IMAGE_MODEL=gemini-flash-image
IMAGE_API_KEY=AIzaSyxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
IMAGE_BASE_URL=https://generativelanguage.googleapis.com/v1beta
```

#### 方法三：通过代码直接配置
在 `src/components/StoryManager.tsx` 中找到图片生成服务配置部分，修改配置：

```javascript
imageGenerationService.setConfig({
  provider: 'openai', // 或 'gemini'
  model: 'gemini-2.5-flash-image', // 或 'gemini-flash-image'
  apiKey: 'sk-43a1OroT63326N3q44D466D33d3a46A19c5d00A6B4Ca9f18',
  baseUrl: 'https://aihubmix.com/v1' // 可选
});
```

## 3. 推荐配置

### 3.1 OpenAI DALL-E 3（推荐）
- **提供商**: openai
- **模型**: dall-e-3
- **优势**: 生成质量高，细节丰富
- **注意**: 成本相对较高

### 3.2 Gemini Flash Image
- **提供商**: gemini
- **模型**: gemini-flash-image
- **优势**: 速度快，成本较低
- **注意**: 需要Google账号

## 4. 验证配置

配置完成后，可以通过以下方式验证：

1. 启动应用
2. 开始一个新的故事
3. 等待选项生成
4. 查看选项是否显示图片
5. 如果图片正常显示，说明配置成功

## 5. 故障排除

### 5.1 图片未显示
1. 检查控制台是否有错误信息
2. 确认API密钥是否正确
3. 检查网络连接是否正常
4. 确认API配额是否充足

### 5.2 图片加载失败
1. 检查图片URL是否有效
2. 尝试刷新页面重新加载
3. 检查防火墙或代理设置

### 5.3 配置不生效
1. 确认配置是否正确保存
2. 检查是否有缓存需要清除
3. 重启应用使配置生效

## 6. 最佳实践

1. **安全存储密钥**: 不要将API密钥提交到版本控制系统
2. **监控使用量**: 定期检查API使用情况和费用
3. **备用方案**: 配置多个服务提供商以备不时之需
4. **测试配置**: 在正式使用前先进行测试