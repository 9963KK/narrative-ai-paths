# 织梦师 (Weaver of Dreams) - 项目架构完整分析

## 📋 项目概述

**项目名称**: 织梦师 (Weaver of Dreams)  
**项目定位**: AI驱动的互动式故事创作平台  
**技术栈**: React 18 + TypeScript + Vite + Supabase  
**开发状态**: 生产环境稳定版本

### 核心特性
- 🤖 多AI模型支持（OpenAI、Claude、DeepSeek、月之暗面、智谱AI等）
- 📖 互动式故事生成与演进
- 🎯 简单/高级两种故事创建模式
- 💾 智能存档系统与上下文管理
- 🏆 用户等级与积分系统
- 📄 文档分析与创意提取
- 🎨 流式内容生成体验

---

## 🏗️ 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                        用户界面层 (UI Layer)                  │
│  Pages (页面) + Components (组件) + Layouts (布局)            │
└────────────────────┬────────────────────────────────────────┘
                     │
┌────────────────────▼────────────────────────────────────────┐
│                     服务层 (Service Layer)                   │
│  • storyAI (故事生成核心)                                     │
│  • unifiedAIService (统一AI调用)                             │
│  • unifiedAuthService (统一认证)                             │
│  • creditService (积分管理)                                   │
│  • userLevelService (等级管理)                               │
│  • configurationManager (配置管理)                           │
└────────────────────┬────────────────────────────────────────┘
                     │
┌────────────────────▼────────────────────────────────────────┐
│                   模块层 (Module Layer)                      │
│  • AIModelService (AI模型服务)                               │
│  • StoryStateManager (故事状态管理)                          │
│  • ContentGenerator (内容生成器)                             │
│  • ChoiceGenerator (选择生成器)                              │
│  • SummaryManager (摘要管理器)                               │
│  • DocumentAnalyzer (文档分析器)                             │
└────────────────────┬────────────────────────────────────────┘
                     │
┌────────────────────▼────────────────────────────────────────┐
│                    数据层 (Data Layer)                       │
│  • Supabase (云数据库)                                        │
│  • LocalStorage (本地存储)                                   │
│  • SessionStorage (会话存储)                                 │
└─────────────────────────────────────────────────────────────┘
```

---

## 📁 目录结构详解

### 1️⃣ **根目录文件**

| 文件 | 功能说明 |
|------|---------|
| `package.json` | 项目配置、依赖管理 |
| `vite.config.ts` | Vite构建配置 |
| `tsconfig.json` | TypeScript编译配置 |
| `tailwind.config.ts` | TailwindCSS样式配置 |
| `.env.example` | 环境变量模板 |
| `vercel.json` | Vercel部署配置 |
| `components.json` | shadcn/ui组件配置 |

### 2️⃣ **src/services/** - 核心服务层

这是项目的**核心业务逻辑层**，包含所有主要服务：

#### 🎯 **故事生成相关**

**`storyAI.ts`** - 故事AI主服务（2000+ 行）
```typescript
class StoryAI {
  // 核心方法：
  - generateInitialStory()        // 生成初始故事
  - generateInitialStoryStream()  // 流式生成初始故事  
  - generateNextChapter()         // 生成下一章节
  - generateNextChapterStream()   // 流式生成章节
  - generateChoices()             // 生成选择项
  - generateStoryEnding()         // 生成故事结局
  - generateStoryOutlines()       // 生成故事大纲
  - analyzeDocument()             // 分析文档
  - developCharacter()            // 发展角色
  
  // 状态管理：
  - getCurrentStoryState()        // 获取当前状态
  - updateStoryState()            // 更新状态
  - resetStoryState()             // 重置状态
  
  // 对话历史：
  - getConversationHistory()      // 获取历史
  - setConversationHistory()      // 设置历史
  - clearConversationHistory()    // 清空历史
}
```

**关键特性**:
- 采用**模块化架构**，整合多个功能模块
- 支持**流式生成**，提供打字机效果
- 自动**上下文摘要**，每6轮对话压缩历史
- **JSON输出模式**确保AI返回格式可靠
- **3次重试机制**保障生成稳定性

**`documentAnalyzer.ts`** - 文档分析服务
```typescript
class DocumentAnalyzer {
  - analyzeDocument()              // 分析文档内容
  - readFile()                     // 读取文件
  - isFileTypeSupported()          // 检查文件类型
  - getSupportedExtensions()       // 获取支持的扩展名
}
```
**功能**: 支持.txt/.md/.pdf/.docx等格式，提取角色、设定、主题、风格等元素

#### 🔐 **认证与用户管理**

**`unifiedAuthService.ts`** - 统一认证服务
```typescript
class UnifiedAuthService {
  // 认证操作：
  - register()                     // 用户注册
  - login()                        // 用户登录
  - logout()                       // 登出
  - getCurrentUser()               // 获取当前用户
  - isAuthenticated()              // 检查认证状态
  
  // 用户管理：
  - updateUser()                   // 更新用户信息
  - changePassword()               // 修改密码
  - deleteAccount()                // 删除账户
  - getAllUsers()                  // 获取所有用户（管理员）
  
  // OAuth支持：
  - signInWithOAuth()              // OAuth登录
  - handleOAuthCallback()          // OAuth回调处理
  - onAuthStateChange()            // 监听认证状态变化
}
```

**特点**:
- 支持**本地存储**和**Supabase云端**双模式
- 自动检测Supabase可用性并降级
- 支持OAuth第三方登录（Google、GitHub等）
- 密码哈希加密存储

**`cloudAuthService.ts`** - 云认证服务（Supabase专用）
- 专门处理Supabase认证逻辑
- 支持后台同步机制
- 提供离线数据同步

**`authService.ts`** - 本地认证服务（备用）
- 完全基于LocalStorage的认证
- 无需网络连接即可使用

#### 💰 **积分系统**

**`creditService.ts`** - 积分管理服务
```typescript
class CreditService {
  // 积分操作：
  - getUserCredits()               // 获取用户积分
  - initializeUserCredits()        // 初始化用户积分
  - calculateRequiredCredits()     // 计算所需积分
  - checkSufficientCredits()       // 检查积分是否充足
  - deductCredits()                // 扣除积分
  - adminAddCredits()              // 管理员充值
  
  // 交易记录：
  - getUserTransactions()          // 获取交易记录
  - getModelRate()                 // 获取模型费率
  - getAllModelRates()             // 获取所有费率
  - getCreditSystemOverview()      // 获取系统概览
}
```

**积分规则**:
- 新用户赠送100积分
- AI调用根据token消耗扣费
- 支持多种AI模型不同费率
- 完整的交易记录追踪

#### 🏆 **用户等级系统**

**`userLevelService.ts`** - 等级管理服务
```typescript
class UserLevelService {
  - getUserLevelPermissions()      // 获取等级权限
  - getAllUsersWithLevel()         // 获取所有用户等级
  - updateUserLevel()              // 更新用户等级
  - batchUpdateUserLevels()        // 批量更新等级
  - getUserLevelChanges()          // 获取等级变更记录
  - getUserAvailableModelsByLevel() // 获取可用模型
  - getUserLevel()                 // 获取用户等级
  - canUserAccessModelLevel()      // 检查模型访问权限
}
```

**等级体系**:
```
- Free (免费版): 基础模型
- Basic (基础版): 更多模型选择
- Pro (专业版): 高性能模型
- Enterprise (企业版): 全部模型 + 自定义
```

#### 🤖 **AI服务集成**

**`unifiedAIService.ts`** - 统一AI调用服务
```typescript
class UnifiedAIService {
  // AI请求：
  - makeRequest()                  // 发起AI请求
  - makeRequestStream()            // 流式AI请求
  
  // 配置管理：
  - getUserModelConfig()           // 获取用户模型配置
  - refreshConfig()                // 刷新配置
  - clearSessionCache()            // 清空会话缓存
  
  // 监控统计：
  - getStats()                     // 获取统计数据
  - resetStats()                   // 重置统计
}
```

**支持的AI提供商**:
- OpenAI (GPT-4, GPT-3.5等)
- Anthropic (Claude系列)
- DeepSeek
- 月之暗面 (Moonshot)
- 智谱AI (GLM)
- OpenRouter（多模型聚合）

**核心功能**:
- 自动积分检查与扣费
- Token使用量估算
- 请求失败重试
- 流式响应支持
- 多提供商统一接口

#### ⚙️ **配置管理**

**`configurationManager.ts`** - 配置管理器
```typescript
class ConfigurationManager {
  // 配置获取：
  - getUserModelConfig()           // 获取用户模型配置
  
  // 缓存管理：
  - clearUserCache()               // 清除用户缓存
  - clearAllCaches()               // 清除所有缓存
  - onUserLogout()                 // 登出时清理
  - forceRefreshConfig()           // 强制刷新配置
  
  // 统计信息：
  - getCacheStats()                // 获取缓存统计
}
```

**缓存策略**:
- 三级缓存：内存 > Session > Database
- 自动TTL过期管理
- 用户登出自动清理

**`modelConfigAdapter.ts`** - 模型配置适配器
```typescript
class ModelConfigAdapter {
  - getUserModelConfig()           // 获取模型配置
  - getUserDisplayModel()          // 获取显示模型
  - getUserAvailableDisplayModels() // 获取可用模型
  - getModelConfigById()           // 根据ID获取配置
  - getRecommendedModel()          // 获取推荐模型
  - logModelUsage()                // 记录使用情况
  - getRealApiKey()                // 获取真实API密钥
}
```

#### 💾 **存储管理**

**`contextManager.ts`** - 上下文存储管理
```typescript
class ContextManager {
  // 存储操作：
  - saveStoryContext()             // 保存故事上下文
  - loadStoryContext()             // 加载故事上下文
  - getSavedContexts()             // 获取已保存列表
  - deleteStoryContext()           // 删除存档
  - renameStoryContext()           // 重命名存档
  
  // 自动保存：
  - saveStoryProgress()            // 保存进度
  - autoSave()                     // 自动保存
  - cleanupAutoSaves()             // 清理自动保存
  - cleanupDuplicates()            // 清理重复
  
  // 导入导出：
  - exportContext()                // 导出存档
  - importContext()                // 导入存档
}
```

**存档结构**:
```typescript
interface SavedStoryContext {
  id: string;                      // 存档ID
  title: string;                   // 存档标题
  thumbnail: string;               // 缩略图
  storyState: StoryState;          // 故事状态
  conversationHistory: any[];      // 对话历史
  summaryData?: SummaryData;       // 摘要数据
  savedAt: string;                 // 保存时间
  version: number;                 // 版本号
}
```

**`userStorage.ts`** - 用户存储服务
```typescript
class UserStorageService {
  - setItem()                      // 存储项目
  - getItem()                      // 获取项目  
  - removeItem()                   // 删除项目
  - clearUserData()                // 清除用户数据
  - clearAllGuestData()            // 清除访客数据
  - getUserDataKeys()              // 获取数据键列表
  - migrateDataToUser()            // 数据迁移
}
```

#### 🔧 **其他服务**

**`modelDiscoveryService.ts`** - 模型发现服务
- 自动发现OpenAI兼容模型
- 自动发现Claude兼容模型
- 验证API密钥有效性

**`userModelConfigService.ts`** - 用户模型配置
- 获取用户可用模型
- 获取用户默认模型
- 系统模型池管理

**`tokenMonitorService.ts`** - Token监控
- 记录Token使用量
- 统计用户Token消耗
- 估算Token成本

**`imageGenerationService.ts`** - 图像生成
- 为故事选择生成配图
- 生成图像提示词

**`adminDataService.ts`** - 管理数据服务
- 获取Dashboard统计
- 获取用户摘要
- 导出数据为CSV

---

### 3️⃣ **src/components/** - 组件层

#### 📖 **故事相关组件**

**`StoryManager.tsx`** - 故事管理器
- 故事流程控制
- 章节导航
- 进度追踪

**`StoryReader.tsx`** - 故事阅读器
- 内容展示（打字机效果）
- 选择项展示与交互
- 氛围与紧张度显示

**`StoryInitializer.tsx`** - 故事初始化器
- 简单/高级配置切换
- 故事大纲生成与选择
- 角色与目标配置

**`SaveManager.tsx`** - 存档管理器
- 手动存档
- 自动存档开关
- 存档列表管理

#### 📄 **文档分析组件**

**`DocumentAnalyzer.tsx`** - 文档分析器
- 文件上传
- 文档解析
- 分析结果展示

**`DocumentAnalysisResultView.tsx`** - 分析结果视图
- 角色信息展示
- 设定与主题展示
- 故事种子展示

**`DocumentRecordManager.tsx`** - 文档记录管理
- 分析历史记录
- 记录搜索与过滤

#### 🎨 **模型配置组件**

**`UserModelSelector.tsx`** - 用户模型选择器
- 模型列表展示
- 等级权限检查
- 模型切换

**`SimpleModelSettings.tsx`** - 简单模型配置
- 快速配置AI模型
- API密钥输入

**`UserLevelModelConfig.tsx`** - 用户等级模型配置
- 根据等级显示可用模型
- 升级提示

**model-config/** 子目录：
- `ModelSelector.tsx` - 模型选择器
- `ApiConfiguration.tsx` - API配置
- `AdvancedSettings.tsx` - 高级设置
- `constants.ts` - 模型常量定义
- `modelService.ts` - 模型服务
- `modelListService.ts` - 模型列表服务

#### 🔐 **认证组件**

**auth/** 子目录：
- `AuthForm.tsx` - 登录/注册表单
- `UserHeader.tsx` - 用户头像与菜单
- `ProtectedRoute.tsx` - 路由保护
- `OAuthCallback.tsx` - OAuth回调处理

#### 🛡️ **管理员组件**

**admin/** 子目录：
- `CreditManagementTab.tsx` - 积分管理标签页
- `ModelManagementTab.tsx` - 模型管理标签页
- `UserLevelManagementTab.tsx` - 用户等级管理标签页

#### 🎯 **UI组件库**

**ui/** 子目录（基于shadcn/ui）：
- `alert.tsx` - 警告组件
- `accordion.tsx` - 手风琴组件
- `CreditBadge.tsx` - 积分徽章
- `CreditHistory.tsx` - 积分历史
- `UserLevelBadge.tsx` - 用户等级徽章
- `StageProgressIndicator.tsx` - 阶段进度指示器
- 等30+个UI组件

#### 🔧 **辅助组件**

- `AnimatedCard.tsx` - 动画卡片
- `ConfettiWrapper.tsx` - 彩纸效果包装器
- `ConfigDebugger.tsx` - 配置调试器
- `DataSyncManager.tsx` - 数据同步管理
- `SyncNotification.tsx` - 同步通知
- `SyncStatusBadge.tsx` - 同步状态徽章

---

### 4️⃣ **src/pages/** - 页面层

| 页面文件 | 路由 | 功能说明 |
|---------|------|---------|
| `Home.tsx` | `/` | 首页，项目介绍 |
| `Index.tsx` | `/index` | 主入口页面 |
| `Login.tsx` | `/login` | 登录页面 |
| `Story.tsx` | `/story` | 故事游玩主页 |
| `StoryCreating.tsx` | `/story/creating` | 故事创建页 |
| `StoryDetail.tsx` | `/story/:id` | 故事详情页 |
| `QuickStart.tsx` | `/quick-start` | 快速开始向导 |
| `Settings.tsx` | `/settings` | 用户设置 |
| `Profile.tsx` | `/profile` | 用户资料 |
| `Advanced.tsx` | `/advanced` | 高级配置 |
| `AdminDashboard.tsx` | `/admin` | 管理员面板 |
| `SaveArchive.tsx` | `/archive` | 存档管理 |
| `DocumentAnalysis.tsx` | `/document-analysis` | 文档分析 |
| `DatabaseTest.tsx` | `/test/database` | 数据库测试 |
| `CreditTest.tsx` | `/test/credit` | 积分测试 |
| `ModelConfigTest.tsx` | `/test/model-config` | 模型配置测试 |
| `NotFound.tsx` | `*` | 404页面 |

---

### 5️⃣ **src/agents/** - Agent系统（实验性）

```
agents/
├── core/
│   └── BaseAgent.ts          # Agent基类
├── roles/
│   └── DirectorAgent.ts      # 导演Agent
└── simple/
    └── StoryWriterAgent.ts   # 故事写作Agent
```

**说明**: 这是一个实验性的Agent系统，用于探索多Agent协作生成故事的可能性。目前尚未完全集成到主流程中。

---

### 6️⃣ **src/contexts/** - React上下文

**`AuthContext.tsx`** - 认证上下文
- 提供全局认证状态
- 用户信息管理
- 登录/登出操作

**`CreditContext.tsx`** - 积分上下文
- 提供全局积分状态
- 积分变更监听
- 积分不足提示

---

### 7️⃣ **src/utils/** - 工具函数

- `logger.ts` - 日志工具
- `modelStoryMatcher.ts` - 模型故事匹配器
- `urlUtils.ts` - URL工具函数

---

### 8️⃣ **src/hooks/** - 自定义Hooks

- `use-toast.ts` - Toast提示Hook
- `use-mobile.tsx` - 移动端检测Hook
- `useCardAnimation.ts` - 卡片动画Hook
- `usePerformanceConfig.ts` - 性能配置Hook

---

### 9️⃣ **sql/** - 数据库脚本

包含Supabase数据库的所有初始化和迁移脚本：

```
sql/
├── 01-supabase-setup.sql              # 初始化设置
├── 02-supabase-admin-fix.sql          # 管理员修复
├── 03-credit-system-setup.sql         # 积分系统设置
├── 04-user-model-config-production.sql # 用户模型配置
├── 05-setup-existing-users.sql        # 现有用户设置
├── 06-database-cleanup.sql            # 数据库清理
├── 07-remove-display-names.sql        # 移除显示名称
├── 08-user-level-system.sql           # 用户等级系统
├── 09-user-level-system-fix.sql       # 等级系统修复
├── 10-safe-migration.sql              # 安全迁移
├── 11-test-user-level-system.sql      # 测试等级系统
├── 12-fix-credit-transaction-amounts.sql # 修复交易金额
└── README.md                          # SQL文档说明
```

---

### 🔟 **docs/** - 文档

| 文档文件 | 内容说明 |
|---------|---------|
| `StoryAI-Architecture.md` | StoryAI模块化架构设计 |
| `AGENT_ARCHITECTURE.md` | Agent系统架构 |
| `AI_DEBUG_GUIDE.md` | AI调试指南 |
| `BRANCH_STRATEGY.md` | Git分支策略 |
| `OAuth_SETUP.md` | OAuth配置说明 |
| `OAuth_TROUBLESHOOTING.md` | OAuth故障排查 |
| `user-model-access-guide.md` | 用户模型访问指南 |
| `credit-system-design.md` | 积分系统设计 |
| `credit-system-test-guide.md` | 积分系统测试指南 |
| `日志系统使用说明.md` | 日志系统说明 |
| `积分交易记录修复说明.md` | 积分修复说明 |

---

## 🔄 数据流图

### 故事生成流程

```
用户输入故事配置
    ↓
StoryInitializer组件
    ↓
storyAI.generateInitialStory()
    ↓
unifiedAIService.makeRequest()
    ├── 检查用户积分 (creditService)
    ├── 获取模型配置 (configurationManager)
    ├── 验证模型权限 (modelAccessValidator)
    └── 调用AI API (OpenAI/Claude/etc)
    ↓
contentParser.parseStoryResponse()
    ↓
storyStateManager.setState()
    ↓
conversationManager.addToHistory()
    ↓
返回故事内容到UI
    ↓
StoryReader展示内容
    ↓
用户做出选择
    ↓
storyAI.generateNextChapter()
    ↓
检查是否需要生成摘要 (summaryManager)
    ↓
contentGenerator.generateNextChapter()
    ↓
... 循环继续 ...
```

### 用户认证流程

```
用户点击登录
    ↓
AuthForm组件
    ↓
unifiedAuthService.login()
    ├── 检查Supabase连接
    │   ├── 可用 → cloudAuthService.login()
    │   └── 不可用 → authService.login() (本地)
    ↓
验证用户名密码
    ↓
生成会话Token
    ↓
保存到Context (AuthContext)
    ↓
初始化用户积分 (creditService)
    ↓
加载用户配置 (configurationManager)
    ↓
跳转到主页
```

### 积分扣费流程

```
AI请求发起
    ↓
unifiedAIService.makeRequest()
    ↓
estimateTokenUsage() - 估算Token消耗
    ↓
creditService.calculateRequiredCredits()
    ↓
creditService.checkSufficientCredits()
    ├── 积分不足 → 返回错误
    └── 积分充足 → 继续
    ↓
执行AI调用
    ↓
获取实际Token消耗
    ↓
creditService.deductCredits()
    ↓
记录交易 (addTransaction)
    ↓
触发积分更新事件
    ↓
CreditContext更新UI
```

---

## 🎯 核心功能模块详解

### 1. 模块化StoryAI架构

基于 `docs/StoryAI-Architecture.md` 设计，将庞大的storyAI服务拆分为多个专门模块：

#### 核心模块

**AIModelService** - AI模型服务
- 统一管理所有AI调用
- Token消耗监控
- 错误处理和重试
- 支持JSON输出模式

**StoryStateManager** - 故事状态管理
- 故事状态持久化
- 状态验证和同步
- 跨会话恢复

#### 功能模块

**ContentGenerator** - 内容生成器
- 章节内容生成
- 场景描述创作
- 对话生成
- 支持流式输出

**ChoiceGenerator** - 选择生成器
- 动态选择项生成（2-5个）
- 难度评估
- 后果预测

**SummaryManager** - 摘要管理器
- 自动摘要生成（每6轮对话）
- 摘要合并优化
- 上下文压缩

**ConversationManager** - 会话管理器
- 对话历史存储
- 上下文窗口管理
- 历史压缩优化

**CharacterDeveloper** - 角色开发器
- 角色创建和发展
- 性格塑造
- 关系管理

**DocumentAnalyzer** - 文档分析器
- 文档内容解析
- 角色信息提取
- 故事背景识别
- 写作风格识别
- 故事种子生成

**EndingGenerator** - 结局生成器
- 结束条件检测
- 结局类型判断（成功/失败/悬念/满意）
- 结局内容生成

**StoryInitializer** - 故事初始化器
- 故事大纲生成
- 初始场景创建
- 角色初始化

### 2. 统一AI服务

**unifiedAIService** 提供了统一的AI调用接口：

```typescript
// 支持的特性
- 多提供商支持（OpenAI/Claude/DeepSeek/Moonshot/GLM等）
- 自动积分检查与扣费
- Token使用量估算
- 请求失败重试机制
- 流式响应支持
- 会话缓存管理
- 详细的统计数据
```

**JSON输出模式支持**:
```typescript
// 支持JSON Mode的提供商
const supportsJsonMode = [
  'openai',      // GPT-4o, GPT-4o-mini等
  'deepseek',    // deepseek-chat
  'openrouter',  // 通过OpenRouter的模型
  'moonshot',    // moonshot-v1系列
  'zhipu'        // glm-4等
];

// 不支持JSON Mode的提供商（使用增强提示词）
const promptBased = [
  'anthropic'    // Claude系列
];
```

### 3. 配置管理系统

**三级缓存架构**:

```
1. 内存缓存 (Memory Cache)
   - 速度最快
   - 会话期间持久
   - TTL默认5分钟

2. Session缓存 (SessionStorage)
   - 标签页级别
   - 浏览器关闭后清除
   - 作为内存缓存的备份

3. 数据库缓存 (Supabase)
   - 跨设备同步
   - 永久存储
   - 最终数据源
```

**配置流程**:
```
用户请求配置
    ↓
检查内存缓存 → 命中则返回
    ↓
检查Session缓存 → 命中则返回并更新内存
    ↓
查询Supabase → 找到则返回并更新两级缓存
    ↓
使用默认配置 → 保存到所有缓存层
```

### 4. 积分与等级系统

#### 积分系统

**积分来源**:
- 新用户注册：100积分
- 管理员充值：按需充值
- 未来可能：完成任务获得积分

**积分消耗**:
```typescript
// 费率示例（每1000 tokens）
{
  'openai/gpt-4o': 5,           // 5积分/1k tokens
  'openai/gpt-4o-mini': 1,      // 1积分/1k tokens
  'anthropic/claude-3': 3,      // 3积分/1k tokens
  'deepseek/deepseek-chat': 1,  // 1积分/1k tokens
}
```

**交易记录**:
```typescript
interface CreditTransaction {
  id: string;
  user_id: string;
  amount: number;               // 正数=充值，负数=消耗
  balance_after: number;        // 交易后余额
  description: string;          // 交易描述
  transaction_type: string;     // 类型：ai_call/admin_add等
  metadata: {
    provider?: string;
    model?: string;
    tokens_used?: number;
  };
  created_at: string;
}
```

#### 等级系统

**等级定义**:
```typescript
type UserLevel = 'free' | 'basic' | 'pro' | 'enterprise';

interface UserLevelPermission {
  level: UserLevel;
  max_models: number;           // 最多可配置模型数
  allowed_performance_levels: string[];  // 允许的性能等级
  features: string[];           // 可用功能
}
```

**等级权限**:
| 等级 | 最大模型数 | 性能等级 | 特殊权限 |
|------|-----------|---------|---------|
| Free | 3 | basic | 基础功能 |
| Basic | 5 | basic, standard | 更多模型 |
| Pro | 10 | basic, standard, high | 高性能模型 |
| Enterprise | 无限制 | 全部 | 自定义模型 |

**模型访问验证**:
```typescript
class ModelAccessValidator {
  // 验证用户是否有权访问指定模型
  static async validateModelAccess(
    modelId: string,
    userId?: string
  ): Promise<boolean>
  
  // 验证等级权限
  static validateLevelAccess(
    modelPerformanceLevel: string,
    userLevel: UserLevel
  ): boolean
  
  // 获取升级建议
  static getUpgradeRecommendation(
    model: ModelByLevel,
    userLevel: UserLevel
  ): string
}
```

### 5. 存档系统

#### 存档类型

**主存档** (Main Archive):
- 每个故事一个唯一ID
- 格式：`story_${story_id}`
- 包含完整故事状态

**自动保存** (Auto Save):
- 每次选择后自动触发
- 可通过开关控制
- 保留最近3个自动存档

**手动存档** (Manual Save):
- 用户手动创建快照
- 可自定义标题
- 无数量限制

#### 存档内容

```typescript
interface SavedStoryContext {
  // 基本信息
  id: string;                        // 存档唯一ID
  title: string;                     // 存档标题
  thumbnail: string;                 // 缩略图（emoji或图标）
  
  // 故事数据
  storyState: StoryState;            // 完整故事状态
  conversationHistory: ConversationHistory[];  // 对话历史
  summaryData?: SummaryData;         // 摘要数据
  
  // 元数据
  savedAt: string;                   // 保存时间
  version: number;                   // 版本号
  genre: string;                     // 故事类型
  chapter: number;                   // 当前章节
  playTime: number;                  // 游玩时长（分钟）
  progress: number;                  // 进度百分比
}
```

#### 存档操作

**保存流程**:
```
1. 生成存档ID（基于story_id）
2. 收集当前故事状态
3. 收集对话历史
4. 收集摘要数据
5. 生成缩略图和元数据
6. 序列化为JSON
7. 保存到LocalStorage
8. 触发同步到Supabase（如果连接）
```

**加载流程**:
```
1. 从LocalStorage读取存档列表
2. 解析JSON数据
3. 验证版本兼容性
4. 恢复故事状态到storyStateManager
5. 恢复对话历史到conversationManager
6. 恢复摘要数据到summaryManager
7. 更新UI显示
```

### 6. 文档分析功能

**DocumentAnalyzer** 支持从现有文档中提取创作灵感：

#### 支持的文件格式

- `.txt` - 纯文本
- `.md` - Markdown
- `.rtf` - 富文本
- `.doc/.docx` - Word文档（需浏览器支持）
- `.pdf` - PDF文档（文本模式）

#### 分析内容

**角色提取**:
```typescript
// 提取真实角色姓名，避免泛指
{
  name: "张三",              // 真实姓名
  type: "protagonist",       // 角色类型
  traits: ["勇敢", "善良"],  // 性格特征
  backstory: "..."          // 背景故事
}
```

**设定提取**:
```typescript
{
  time: "现代都市",          // 时代背景
  place: "上海",             // 地理位置
  worldBackground: "...",    // 世界观
  atmosphere: "紧张刺激"     // 氛围
}
```

**主题分析**:
```typescript
{
  mainThemes: ["友情", "成长"],  // 主要主题
  deeperMeaning: "..."           // 深层含义
}
```

**风格识别**:
```typescript
{
  tone: "幽默轻松",              // 语调
  narrativePerspective: "第一人称",  // 视角
  genre: "都市奇幻"              // 文体
}
```

**故事种子生成**:
```typescript
// 基于分析结果生成3-4个故事方向
[
  {
    title: "都市修仙传",
    premise: "现代都市中的修仙者...",
    characters: ["李明", "王芳"],
    setting: "现代上海"
  },
  // ... 更多种子
]
```

#### 分析流程

```
1. 用户上传文档
   ↓
2. 读取文件内容（readFile）
   ↓
3. 发送到AI进行分析
   ├── 提取角色信息（多轮重试确保质量）
   ├── 识别故事设定
   ├── 分析主题和情节
   └── 识别写作风格
   ↓
4. 生成故事种子建议
   ↓
5. 返回结构化分析结果
   ↓
6. 保存到DocumentRecordManager
   ↓
7. 用户可选择种子创建故事
```

---

## 🔑 关键技术点

### 1. 流式内容生成

**实现原理**:
```typescript
// 使用Server-Sent Events (SSE)
async function* streamResponse(response: Response) {
  const reader = response.body!.getReader();
  const decoder = new TextDecoder();
  
  while (true) {
    const { done, value } = await reader.read();
    if (done) break;
    
    const chunk = decoder.decode(value);
    const lines = chunk.split('\n');
    
    for (const line of lines) {
      if (line.startsWith('data: ')) {
        const data = JSON.parse(line.slice(6));
        yield data.choices[0].delta.content;
      }
    }
  }
}

// 在组件中使用
for await (const token of streamResponse(response)) {
  onToken?.(token);  // 实时回调每个token
}
```

**优势**:
- 用户体验更好（打字机效果）
- 感知延迟更低
- 可以提前展示部分内容

### 2. 上下文摘要压缩

**触发条件**:
```typescript
// 每6轮对话触发一次摘要
if (conversationHistory.length >= lastSummaryIndex + 6) {
  await generateSummary();
}
```

**摘要内容**:
```typescript
interface SummaryData {
  plot_developments: string[];      // 剧情发展
  character_changes: Array<{        // 角色变化
    name: string;
    change: string;
  }>;
  key_decisions: Array<{            // 关键决策
    decision: string;
    consequence: string;
  }>;
  atmosphere: {                     // 氛围
    mood: string;
    tension_level: number;
  };
  important_clues: string[];        // 重要线索
}
```

**压缩策略**:
```
原始历史：[msg1, msg2, ..., msg100]  (约50k tokens)
        ↓ 生成摘要
压缩后：[summary] + [msg93-msg100]    (约5k tokens)

节省Token: 90%
保持连贯性: 通过结构化摘要
```

### 3. JSON输出模式

**OpenAI示例**:
```typescript
const response = await fetch(apiUrl, {
  method: 'POST',
  headers: {
    'Authorization': `Bearer ${apiKey}`,
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    model: 'gpt-4o-mini',
    messages: [...],
    response_format: { type: 'json_object' }  // 关键参数
  })
});
```

**提示词增强**（不支持JSON模式的提供商）:
```typescript
const jsonPrompt = `
你必须严格按照以下JSON格式输出，不要添加任何其他文字：

{
  "scene": "故事内容...",
  "chapter_title": "章节标题",
  "mood": "氛围",
  "tension_level": 5
}

【重要】输出必须是有效的JSON，不要使用markdown代码块。
`;
```

### 4. 多层错误恢复

**重试机制**:
```typescript
async function callWithRetry(fn: Function, maxRetries = 3) {
  for (let i = 0; i < maxRetries; i++) {
    try {
      return await fn();
    } catch (error) {
      console.log(`🔄 第${i + 1}次重试...`);
      
      if (i === maxRetries - 1) {
        // 最后一次失败，返回默认内容
        return getDefaultContent();
      }
      
      // 等待后重试
      await delay(1000 * (i + 1));
    }
  }
}
```

**JSON修复**:
```typescript
function repairMalformedJSON(jsonString: string): string {
  // 1. 移除markdown代码块
  jsonString = jsonString.replace(/```json\n?/g, '');
  jsonString = jsonString.replace(/```\n?/g, '');
  
  // 2. 修复常见错误
  jsonString = jsonString
    .replace(/,\s*}/g, '}')      // 移除尾随逗号
    .replace(/,\s*]/g, ']')
    .replace(/'/g, '"')           // 单引号转双引号
    .trim();
  
  // 3. 尝试解析
  try {
    JSON.parse(jsonString);
    return jsonString;
  } catch {
    // 4. 更复杂的修复逻辑...
    return fixComplexIssues(jsonString);
  }
}
```

### 5. 权限验证

**模型访问控制**:
```typescript
// 检查用户等级是否允许访问模型
const userLevel = await userLevelService.getUserLevel(userId);
const modelLevel = model.performance_level;

if (!canAccessLevel(userLevel, modelLevel)) {
  throw new Error('您的等级无法使用此模型');
}

// 等级映射
function canAccessLevel(userLevel: UserLevel, modelLevel: string) {
  const levelMap = {
    'free': ['basic'],
    'basic': ['basic', 'standard'],
    'pro': ['basic', 'standard', 'high'],
    'enterprise': ['basic', 'standard', 'high', 'premium']
  };
  
  return levelMap[userLevel].includes(modelLevel);
}
```

---

## 💡 最佳实践

### 1. 服务层设计

**单一职责原则**:
- 每个服务类只负责一个明确的功能领域
- `storyAI` 只管理故事生成
- `creditService` 只管理积分
- `authService` 只管理认证

**依赖注入**:
```typescript
// 避免硬编码依赖
class StoryAI {
  constructor(
    private aiService = unifiedAIService,
    private stateManager = storyStateManager,
    private conversationMgr = conversationManager
  ) {}
}
```

### 2. 状态管理

**单一数据源**:
- 所有故事状态由 `storyStateManager` 管理
- 不要在组件中直接修改状态
- 通过 `updateState()` 方法统一更新

**状态同步**:
```typescript
// ❌ 错误：直接修改
storyState.chapter = 2;

// ✅ 正确：通过管理器更新
storyStateManager.updateState({ chapter: 2 });
```

### 3. 错误处理

**异步操作**:
```typescript
// 总是使用try-catch包裹异步调用
try {
  const result = await storyAI.generateNextChapter(...);
  // 处理成功结果
} catch (error) {
  console.error('生成失败:', error);
  // 提供降级方案
  showDefaultContent();
}
```

**用户友好的错误消息**:
```typescript
// ❌ 技术错误
"TypeError: Cannot read property 'map' of undefined"

// ✅ 用户友好
"故事生成遇到问题，请稍后重试"
```

### 4. 性能优化

**缓存策略**:
- 频繁访问的数据使用内存缓存
- 会话数据使用SessionStorage
- 用户配置使用Supabase

**懒加载**:
```typescript
// 路由级别的代码分割
const AdminDashboard = lazy(() => import('./pages/AdminDashboard'));

// 条件加载大型依赖
if (needsAnalysis) {
  const { analyzeDocument } = await import('./services/documentAnalyzer');
}
```

**防抖与节流**:
```typescript
// 自动保存使用防抖
const debouncedSave = debounce(autoSave, 2000);

// 滚动事件使用节流
const throttledScroll = throttle(handleScroll, 100);
```

### 5. 类型安全

**严格的TypeScript配置**:
```json
{
  "strict": true,
  "noImplicitAny": true,
  "strictNullChecks": true
}
```

**接口定义**:
```typescript
// 明确的类型定义
interface StoryConfig {
  genre: string;
  setting?: string;
  characters?: Character[];
  story_idea?: string;
  main_goal?: string;
}

// 避免any类型
// ❌ function process(data: any): any
// ✅ function process(data: StoryConfig): StoryState
```

---

## 📊 数据库设计

### Supabase表结构

**users** - 用户表
```sql
CREATE TABLE users (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  username TEXT UNIQUE NOT NULL,
  email TEXT UNIQUE NOT NULL,
  password_hash TEXT NOT NULL,
  role TEXT DEFAULT 'user',
  user_level TEXT DEFAULT 'free',
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);
```

**user_credits** - 用户积分表
```sql
CREATE TABLE user_credits (
  user_id UUID PRIMARY KEY REFERENCES users(id),
  balance INTEGER DEFAULT 100,
  total_earned INTEGER DEFAULT 100,
  total_spent INTEGER DEFAULT 0,
  updated_at TIMESTAMP DEFAULT NOW()
);
```

**credit_transactions** - 积分交易表
```sql
CREATE TABLE credit_transactions (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID REFERENCES users(id),
  amount INTEGER NOT NULL,
  balance_after INTEGER NOT NULL,
  description TEXT,
  transaction_type TEXT,
  metadata JSONB,
  created_at TIMESTAMP DEFAULT NOW()
);
```

**user_model_configs** - 用户模型配置表
```sql
CREATE TABLE user_model_configs (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID REFERENCES users(id),
  model_id TEXT NOT NULL,
  is_default BOOLEAN DEFAULT FALSE,
  api_key_encrypted TEXT,
  base_url TEXT,
  created_at TIMESTAMP DEFAULT NOW()
);
```

**ai_model_rates** - AI模型费率表
```sql
CREATE TABLE ai_model_rates (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  provider TEXT NOT NULL,
  model TEXT NOT NULL,
  rate_per_1k_tokens NUMERIC(10,2),
  performance_level TEXT,
  min_user_level TEXT DEFAULT 'free',
  created_at TIMESTAMP DEFAULT NOW()
);
```

---

## 🚀 部署架构

### Vercel部署

**项目配置** (`vercel.json`):
```json
{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/$1"
    },
    {
      "src": "/(.*)",
      "dest": "/"
    }
  ]
}
```

**环境变量**:
```
VITE_SUPABASE_URL=https://xxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJxxx...
VITE_APP_VERSION=2.0.0
```

### Supabase配置

**Row Level Security (RLS)** 策略:
```sql
-- 用户只能查看自己的积分
CREATE POLICY "Users can view own credits"
  ON user_credits FOR SELECT
  USING (auth.uid() = user_id);

-- 用户只能查看自己的交易
CREATE POLICY "Users can view own transactions"
  ON credit_transactions FOR SELECT
  USING (auth.uid() = user_id);

-- 管理员可以查看所有数据
CREATE POLICY "Admins can view all"
  ON users FOR ALL
  USING (
    EXISTS (
      SELECT 1 FROM users
      WHERE id = auth.uid() AND role = 'admin'
    )
  );
```

---

## 🎓 学习路径建议

### 新手入门

1. **了解基础架构**
   - 阅读 `README.md`
   - 查看 `package.json` 了解依赖
   - 运行项目看基本功能

2. **理解核心流程**
   - 从 `src/pages/Story.tsx` 开始
   - 跟踪一个完整的故事生成流程
   - 理解组件如何调用服务

3. **熟悉服务层**
   - 研究 `storyAI.ts` 的主要方法
   - 了解 `unifiedAIService` 如何调用AI
   - 掌握积分和认证服务

### 进阶开发

1. **模块化架构**
   - 阅读 `docs/StoryAI-Architecture.md`
   - 理解各个功能模块的职责
   - 学习如何添加新模块

2. **数据流理解**
   - 追踪用户操作到AI响应的完整流程
   - 理解状态如何在各层传递
   - 掌握缓存策略

3. **扩展功能**
   - 添加新的AI提供商
   - 实现新的故事类型
   - 开发新的分析功能

---

## 🔧 常见问题

### Q1: 如何添加新的AI提供商？

```typescript
// 1. 在 unifiedAIService.ts 中添加API端点
private getAPIEndpoint(baseUrl: string): string {
  if (baseUrl.includes('your-provider')) {
    return `${baseUrl}/chat/completions`;
  }
  // ...
}

// 2. 在 constants.ts 中注册提供商
export const AI_PROVIDERS = [
  // ...
  {
    id: 'your-provider',
    name: 'Your Provider',
    models: ['model-1', 'model-2']
  }
];

// 3. 配置JSON模式支持
const supportsJsonMode = provider === 'your-provider';
```

### Q2: 如何调试AI调用问题？

```typescript
// 开启详细日志
localStorage.setItem('DEBUG_AI', 'true');

// 查看完整的请求和响应
console.log('AI Request:', request);
console.log('AI Response:', response);

// 使用浏览器开发者工具的Network标签
// 查看实际的HTTP请求
```

### Q3: 如何优化Token消耗？

1. **使用摘要系统**：自动压缩历史对话
2. **选择合适的模型**：简单任务用小模型
3. **控制上下文长度**：只保留必要的历史
4. **缓存常见响应**：避免重复调用

### Q4: 如何处理大文件分析？

```typescript
// 文档分段处理
const maxChunkSize = 10000; // 字符数
if (content.length > maxChunkSize) {
  const chunks = splitIntoChunks(content, maxChunkSize);
  const results = await Promise.all(
    chunks.map(chunk => analyzeChunk(chunk))
  );
  return mergeResults(results);
}
```

---

## 📈 未来规划

### 短期计划（1-3个月）

- [ ] 完善Agent协作系统
- [ ] 增加更多AI提供商支持
- [ ] 优化移动端体验
- [ ] 增加故事分享功能
- [ ] 实现多人协作创作

### 中期计划（3-6个月）

- [ ] AI配图功能完善
- [ ] 语音朗读功能
- [ ] 故事导出为电子书
- [ ] 社区功能（点赞、评论）
- [ ] 高级分析工具

### 长期愿景（6-12个月）

- [ ] 多语言支持
- [ ] 移动端APP
- [ ] AI训练定制化
- [ ] 专业创作工具集
- [ ] 变现模式探索

---

## 📝 总结<

**项目优势**:
✅ 模块化架构清晰，易于维护和扩展  
✅ 完善的用户系统（认证、积分、等级）  
✅ 强大的AI集成能力，支持多种模型  
✅ 智能的上下文管理和摘要系统  
✅ 良好的用户体验（流式生成、自动保存）  
✅ 文档分析功能独特，辅助创作

**技术亮点**:
- 🎯 统一的AI服务接口
- 💾 三级缓存架构
- 🔄 流式内容生成
- 📊 智能上下文压缩
- 🛡️ 多层错误恢复
- 🎨 模块化StoryAI设计

**代码质量**:
- TypeScript严格类型检查
- 清晰的服务层划分
- 完整的错误处理
- 详细的日志记录
- 良好的代码注释

这是一个**架构优秀、功能完善、扩展性强**的AI故事创作平台，适合继续深入开发和商业化探索。

---

**文档版本**: v1.0  
**最后更新**: 2025-11-23  
**维护者**: 织梦师团队
